import {
  app,
  css,
  components,
  modules,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
} from './config';

export default defineNuxtConfig({
  app,
  css,
  components,
  modules,
  plugins,
  runtimeConfig,
  ssr,
  typescript,
});
