packages:
  - apps/client
  - apps/docs
  - apps/marketing
  - benchmarks

# Force the entire workspace onto Vite 8 even though @nuxt/vite-builder
# (Nuxt 4.4.5) declares a "vite": "^7.3.2" peer dependency. Without this
# override pnpm honors the peer dep and resolves Vite 7 alongside Vite 8,
# leaving Nuxt on the older runtime. MUST live at workspace root, NOT in
# package.json's pnpm.overrides — pnpm v9+ in workspace mode silently
# ignores that location and the install completes in <1 s doing nothing
# instead of 10-15 s doing real re-resolution.
overrides:
  vite: ^8.0.14

# pnpm 11's supply-chain policy defaults to a 24-hour minimumReleaseAge,
# which blocks legitimate routine dep bumps when CI runs within a day of
# an upstream patch release (e.g. cloudflare/wrangler, cloudflare/miniflare).
# 60 minutes still prevents publish-and-pivot attacks but unblocks normal flow.
minimumReleaseAge: 60

# pnpm 11 blocks all package build scripts by default. The allowBuilds
# map below is security-sensitive — true = postinstall runs; false = skipped.
# Warning: pnpm auto-manages this block. Edits made while pnpm install is
# running may be clobbered. If you see an autogenerated duplicate allowBuilds
# block, remove the duplicate and keep THIS one as source of truth.
#   - better-sqlite3: native SQLite binding used by @nuxt/content (apps/docs)
#   - esbuild: Vite/Nuxt bundler, verifies prebuilt binary matches platform
#   - sharp: image processing for @nuxt/fonts OG image generation
#   - unrs-resolver: Nuxt 4 module resolver, rust-native platform-specific
#   - workerd: Cloudflare Workers runtime used by apps/marketing wrangler
#   - @parcel/watcher: falls back to polling; safe to skip native build
#   - vue-demi: alias/symlink script; not required at runtime
allowBuilds:
  '@parcel/watcher': false
  better-sqlite3: true
  esbuild: true
  sharp: true
  unrs-resolver: true
  vue-demi: false
  workerd: true

# Catalogs are referenced from workspace packages as `catalog:default`
# (or shorthand `catalog:` for the default). Keep `catalog:default` refs in
# package.json files to match the pre-existing Bun catalog syntax — pnpm
# accepts both conventions.
catalogs:
  default:
    '@commitlint/cli': ^21.0.2
    '@commitlint/config-conventional': ^21.0.2
    '@iconify-json/simple-icons': ^1.2.85
    '@nuxt/eslint': ^1.15.2
    '@nuxt/eslint-config': ^1.15.2
    '@nuxt/fonts': ^0.14.0
    '@nuxt/icon': ^2.2.2
    '@nuxtjs/color-mode': ^4.0.0
    '@tailwindcss/vite': ^4.3.0
    '@types/node': ^25.9.1
    '@vueuse/core': ^14.3.0
    '@vueuse/nuxt': ^14.3.0
    class-variance-authority: ^0.7.1
    clsx: ^2.1.1
    eslint: ^10.4.1
    eslint-plugin-better-tailwindcss: ^4.5.0
    eslint-plugin-oxlint: ^1.68.0
    husky: ^9.1.7
    is-ci: ^4.1.0
    lint-staged: ^17.0.7
    '@lucide/vue': ^1.17.0
    maplibre-gl: ^5.24.0
    motion-v: ^2.2.1
    nuxt: ^4.4.7
    oxfmt: ^0.52.0
    oxlint: ^1.67.0
    radix-vue: ^1.9.17
    reka-ui: ^2.9.9
    shadcn-nuxt: ^2.7.3
    tailwind-merge: ^3.6.0
    tailwindcss: ^4.3.0
    tailwindcss-animate: ^1.0.7
    tw-animate-css: ^1.4.0
    typescript: ^6.0.3
    vite: ^8.0.14
    vue: ^3.5.35
    vue-tsc: ^3.3.3
  client:
    '@comark/nuxt': ^0.3.1
    '@deck.gl/aggregation-layers': ^9.3.2
    '@deck.gl/core': ^9.3.3
    '@deck.gl/geo-layers': ^9.3.2
    '@deck.gl/layers': ^9.3.2
    '@deck.gl/mapbox': ^9.3.2
    '@deck.gl/mesh-layers': ^9.3.2
    '@geoql/v-maplibre': ^1.11.0
    '@luma.gl/engine': ~9.3.3
    '@maplibre/mlt': ^1.1.11
    '@mlc-ai/web-llm': ^0.2.84
    '@tanstack/ai': ^0.23.1
    '@tanstack/ai-vue': ^0.13.3
    '@tanstack/query-db-collection': ^1.0.38
    '@tanstack/vue-db': ^0.0.118
    '@tanstack/vue-query': ^5.101.0
    '@tmcw/togeojson': ^7.1.2
    '@types/geojson': ^7946.0.16
    '@types/papaparse': ^5.5.2
    '@types/shpjs': ^3.4.7
    destr: ^2.0.5
    maplibre-gl-inspect: ^0.0.8
    papaparse: ^5.5.3
    pmtiles: ^4.4.1
    shpjs: ^6.2.0
    zod: ^4.4.3
  marketing:
    '@nuxtjs/plausible': ^3.0.2
    ogl: ^1.0.11
    wrangler: ^4.95.0
  docs:
    '@nuxt/content': ^3.14.0
    better-sqlite3: ^12.10.0
  benchmarks:
    autocannon: ^8.0.0
    chalk: ^5.6.2
    cli-table3: ^0.6.5
    commander: ^15.0.0
